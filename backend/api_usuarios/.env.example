DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
MAIL_USER=
MAIL_PASSWORD=
CONTACT_EMAIL=

# JWT - autenticacion
# Clave secreta para firmar los tokens. Genera una propia distinta por entorno,
# por ejemplo con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=
# Minutos de validez del token (1440 = 24 horas)
JWT_EXPIRE_MINUTES=1440